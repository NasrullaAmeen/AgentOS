[Unit]
Description=AgentOS daily timer
Requires=agent-os.service

[Timer]
# Default: daily at 06:00 UTC. Override with:
#   systemctl edit agent-os.timer
#   [Timer]
#   OnCalendar=*-*-* 09:00:00
OnCalendar=daily
Persistent=true

[Install]
WantedBy=timers.target
